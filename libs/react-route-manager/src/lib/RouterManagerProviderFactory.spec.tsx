describe('RouterManagerProviderFactory', () => {
  describe('BrowerRouter Detection', () => {
    it.todo(
      'Wraps the Router in a BrowserProvider if one is not set when "useLocation" throws an error'
    );
    it.todo('Uses the existing BrowserProvider');
    it.todo('Does not attempt to check the location on state update');
  });
});
