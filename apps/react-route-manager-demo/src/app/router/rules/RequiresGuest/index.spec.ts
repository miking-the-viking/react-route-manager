import { RequiresGuest } from ".";
// import { AppState } from '@hauthura/ui-state';

describe("Requires Guest Route Guard", () => {
  it.todo("duh");
  // it('Returns false if Auth.jwt is truthy', () => {
  //   const validAuthState = {
  //     Auth: {
  //       jwt: 'something',
  //     },
  //   } as AppState;
  //   expect(RequiresGuest(validAuthState)).toBeFalsy();
  // });
  // it('Returns true if Auth.jwt is falsy', () => {
  //   const nullJwtState = {
  //     Auth: {
  //       jwt: null,
  //     },
  //   } as AppState;
  //   expect(RequiresGuest(nullJwtState)).toBeTruthy();
  // });

  // it('Returns true if Auth is falsy', () => {
  //   const nullAuthState = {
  //     Auth: null,
  //   };
  //   expect(RequiresGuest(nullAuthState as AppState)).toBeTruthy();
  // });
});
