import { RequiresAuth } from ".";
import { AppState } from "@react-route-manager/ui-state";

describe("Requires Auth Route Guard", () => {
  it.todo("duh");
  // it("Returns true if Auth.jwt is truthy", () => {
  //   const validAuthState = {
  //     Auth: {
  //       jwt: "something",
  //     },
  //   } as AppState;
  //   expect(RequiresAuth(validAuthState)).toBeTruthy();
  // });
  // it("Returns false if Auth.jwt is falsy", () => {
  //   const nullJwtState = {
  //     Auth: {
  //       jwt: null,
  //     },
  //   } as AppState;
  //   expect(RequiresAuth(nullJwtState)).toBeFalsy();
  // });

  // it("Returns false if Auth is falsy", () => {
  //   const nullAuthState = {
  //     Auth: null,
  //   } as AppState;
  //   expect(RequiresAuth(nullAuthState)).toBeFalsy();
  // });
});
